twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null

    # integration bootstrap des formulaires
    form_themes: ['form/input_file.html.twig']

